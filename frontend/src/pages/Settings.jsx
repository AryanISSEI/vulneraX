import React from 'react';
import PlatformPlaceholder from '../components/PlatformPlaceholder';

export default function Settings() {
  return (
    <PlatformPlaceholder
      eyebrow="Configuration"
      title="Settings"
      description="Manage platform preferences and integration keys."
    />
  );
}
